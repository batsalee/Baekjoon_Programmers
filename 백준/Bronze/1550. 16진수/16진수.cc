#include <bits/stdc++.h>
using namespace std;

void solve()
{
	int num;
	scanf("%x", &num);
	printf("%d", num);
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}