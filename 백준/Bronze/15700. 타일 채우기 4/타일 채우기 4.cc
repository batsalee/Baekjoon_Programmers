#include <bits/stdc++.h>
using namespace std;

void solve()
{
	long long N, M;
	cin >> N >> M;
	cout << N * M / 2;
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}