#include <bits/stdc++.h>
using namespace std;

void solve()
{
	int W, H;
	cin >> W >> H;

	cout.precision(1);
	cout << fixed;
	cout << W * H / 2.0;
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}