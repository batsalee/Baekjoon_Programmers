#include <bits/stdc++.h>
using namespace std;

void solve()
{
	int N;
	cin >> N;

	int a, b;
	while (N--) {
		cin >> a >> b;
		cout << a + b << '\n';
	}
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}