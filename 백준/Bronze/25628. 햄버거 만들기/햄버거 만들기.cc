#include <bits/stdc++.h>
using namespace std;

void solve()
{
	int A, B;
	cin >> A >> B;
	cout << min(A / 2, B);
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}