#include <bits/stdc++.h>
using namespace std;

void solve()
{
	int N;
	cin >> N;
	cout << floor(sqrt(N));
}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solve();

	return 0;
}